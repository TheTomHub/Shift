export function initPostHog() {
  if (process.env.NEXT_PUBLIC_POSTHOG_KEY) {
    // Placeholder for PostHog init.
    // Example: posthog.init(process.env.NEXT_PUBLIC_POSTHOG_KEY, { api_host: 'https://app.posthog.com' });
  }
}
